PORT=3001
# URL(s) autorisée(s) pour le CORS Socket.io côté client déployé (Netlify, séparées par des virgules).
# En local, http://localhost:5173 est déjà autorisé automatiquement.
CLIENT_ORIGIN=https://votre-app.netlify.app
